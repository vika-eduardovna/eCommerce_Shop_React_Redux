//import { getProducts } from "../../requests/categ_req";
import { data } from "../../data";
const defaultState = data;

export const categoriesReducer = (state = defaultState) => { return defaultState }